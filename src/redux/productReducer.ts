const initialState = {
  cart: [],
};

const cartReducer = (state = initialState, action: { type: any; }) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cartReducer;
